import {StyleSheet, Text, View} from 'react-native';
import React from 'react';
import Current from './components/redux/Current';

const App = () => {
  return <Current />;
};

export default App;

const styles = StyleSheet.create({});
